(도메인/기능별 핵심 코드) 가장 중요 게시글(posts), 인증(auth), 댓글(comments) 등 비즈니스 기능(도메인) 단위로 폴더를 나누어 코드를 뭉쳐둠
- 특정 기능 안에는 그 기능에서만 쓰이는 전용 components/, 전용 hooks/, 백엔드 API를 호출하는 services/, 전용 types/ 폴더가 모두 들어감
- index.ts (Public API): 폴더 최상단에 두고, 외부(app 폴더 등)에서 써도 되는 컴포넌트나 함수만 명시적으로 export하여 의존성이 꼬이는 것을 방지